import { FormEvent,useState } from 'react';
import { Link,useNavigate } from 'react-router-dom';
import { Search,ShieldCheck,ArrowRight } from 'lucide-react';
import { properties } from './data';
export default function Home(){const nav=useNavigate();const[q,setQ]=useState('');const submit=(e:FormEvent)=>{e.preventDefault();nav('/properties?city='+encodeURIComponent(q))};return <main><section className="hero"><div><span className="eyebrow"><ShieldCheck size={15}/> Verified homes, clearer renting</span><h1>Find a place.<br/><em>Rent with confidence.</em></h1><p>Discover rental properties, connect with owners, schedule visits, and keep your rental journey organized in one place.</p><div className="actions"><Link className="button" to="/properties">Find a property <ArrowRight size={17}/></Link><Link className="button ghost" to="/register">Post your property</Link></div></div><img className="hero-img" src={properties[0].image} alt="Modern apartment"/></section><section className="search-panel"><strong><Search size={18}/> What are you looking for?</strong><form onSubmit={submit}><label>Location<input value={q} onChange={e=>setQ(e.target.value)} placeholder="City or area"/></label><label>Property type<select><option>Any type</option><option>Apartment</option><option>Flat</option><option>House</option></select></label><label>Budget<input type="number" placeholder="Max rent"/></label><button className="button"><Search size={16}/> Search</button></form></section><section className="section"><span className="kicker">Explore homes</span><div className="section-title"><h2>Places worth coming home to.</h2><Link to="/properties">View all <ArrowRight size={15}/></Link></div><div className="property-grid">{properties.map(p=><article className="property-card" key={p.id}><img src={p.image} alt={p.title}/><div><b>₹{p.rent.toLocaleString('en-IN')}<small>/month</small></b><h3>{p.title}</h3><p>{p.area}, {p.city} · {p.bedrooms} bed · {p.bathrooms} bath</p><Link to={'/properties/'+p.id}>View details →</Link></div></article>)}</div></section></main>}
