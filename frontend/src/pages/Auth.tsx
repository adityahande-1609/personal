import {Link} from 'react-router-dom';
export default function Auth({register=false}:{register?:boolean}){return <main className="auth"><form className="auth-card"><span className="kicker">Rentwise</span><h1>{register?'Create your account':'Welcome back'}</h1><p>{register?'Start finding or managing rental properties.':'Log in to continue your rental journey.'}</p>{register&&<label>Full name<input required placeholder="Your name"/></label>}<label>Email<input required type="email" placeholder="you@example.com"/></label><label>Password<input required type="password" minLength={8} placeholder="At least 8 characters"/></label><button className="button full">{register?'Create account':'Log in'}</button><p>{register?'Already have an account? ':'New here? '}<Link to={register?'/login':'/register'}>{register?'Log in':'Create an account'}</Link></p></form></main>}
