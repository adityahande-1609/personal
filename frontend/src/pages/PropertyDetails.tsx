import { Link,useParams } from 'react-router-dom';import {ShieldCheck,MapPin} from 'lucide-react';import {properties} from './data';
export default function PropertyDetails(){const{id}=useParams();const p=properties.find(x=>x.id===Number(id))||properties[0];return <main className="page details"><Link to="/properties">← Back to properties</Link><div className="detail-grid"><div><img className="detail-img" src={p.image} alt={p.title}/><h2>About this home</h2><p>A thoughtfully maintained home in {p.area}, designed for a comfortable rental experience. Exact residential address information remains protected until appropriately authorized.</p></div><aside><span className="tag">{p.verified&&<><ShieldCheck size={14}/> Verified property</>}</span><h1>{p.title}</h1><p><MapPin size={15}/> {p.area}, {p.city}</p><strong className="big-price">₹{p.rent.toLocaleString('en-IN')}<small>/month</small></strong><div className="specs"><span>{p.bedrooms} Bedrooms</span><span>{p.bathrooms} Bathrooms</span></div><button className="button full">Send enquiry</button><button className="button ghost full">Request a visit</button></aside></div></main>}
